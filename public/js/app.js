// Generated by CoffeeScript 1.9.3
(function() {
  $(function() {
    var hour, minutes, parseTime;
    parseTime = setInterval(function() {
      var date, hourWord, minuteWord;
      date = new Date();
      hourWord = (function() {
        switch (false) {
          case !(hour(date) === 12 && minutes(date) >= 35):
            return "one";
          case !(hour(date) === 1 && minutes(date) < 35):
            return "one";
          case !(hour(date) === 1 && minutes(date) >= 35):
            return "two";
          case !(hour(date) === 2 && minutes(date) < 35):
            return "two";
          case !(hour(date) === 2 && minutes(date) >= 35):
            return "three";
          case !(hour(date) === 3 && minutes(date) < 35):
            return "three";
          case !(hour(date) === 3 && minutes(date) >= 35):
            return "four";
          case !(hour(date) === 4 && minutes(date) < 35):
            return "four";
          case !(hour(date) === 4 && minutes(date) >= 35):
            return "five";
          case !(hour(date) === 5 && minutes(date) < 35):
            return "five";
          case !(hour(date) === 5 && minutes(date) >= 35):
            return "six";
          case !(hour(date) === 6 && minutes(date) < 35):
            return "six";
          case !(hour(date) === 6 && minutes(date) >= 35):
            return "seven";
          case !(hour(date) === 7 && minutes(date) < 35):
            return "seven";
          case !(hour(date) === 7 && minutes(date) >= 35):
            return "eight";
          case !(hour(date) === 8 && minutes(date) < 35):
            return "eight";
          case !(hour(date) === 8 && minutes(date) >= 35):
            return "nine";
          case !(hour(date) === 9 && minutes(date) < 35):
            return "nine";
          case !(hour(date) === 9 && minutes(date) >= 35):
            return "ten";
          case !(hour(date) === 10 && minutes(date) < 35):
            return "ten";
          case !(hour(date) === 10 && minutes(date) >= 35):
            return "eleven";
          case !(hour(date) === 11 && minutes(date) < 35):
            return "eleven";
          case !(hour(date) === 11 && minutes(date) >= 35):
            return "twelve";
          case !(hour(date) === 12 && minutes(date) < 35):
            return "twelve";
        }
      })();
      minuteWord = (function() {
        switch (false) {
          case !(minutes(date) >= 0 && minutes(date) < 5):
            return "oclock";
          case !(minutes(date) >= 5 && minutes(date) < 10):
            return "five-past";
          case !(minutes(date) >= 10 && minutes(date) < 15):
            return "ten-past";
          case !(minutes(date) >= 15 && minutes(date) < 20):
            return "quarter-past";
          case !(minutes(date) >= 20 && minutes(date) < 25):
            return "twenty-past";
          case !(minutes(date) >= 25 && minutes(date) < 30):
            return "twenty-five-past";
          case !(minutes(date) >= 30 && minutes(date) < 35):
            return "half-past";
          case !(minutes(date) >= 35 && minutes(date) < 40):
            return "twenty-five-to";
          case !(minutes(date) >= 40 && minutes(date) < 45):
            return "twenty-to";
          case !(minutes(date) >= 45 && minutes(date) < 50):
            return "quarter-to";
          case !(minutes(date) >= 50 && minutes(date) < 55):
            return "ten-to";
          case !(minutes(date) >= 55 && minutes(date) < 60):
            return "five-to";
        }
      })();
      $("h1:not('.prompt')").removeClass('active');
      $("." + minuteWord + ", ." + hourWord).addClass('active');
      return console.log(minuteWord + " " + hourWord);
    }, 1000);
    hour = function(date) {
      if (date.getHours() === 0) {
        return 12;
      } else if (date.getHours() > 12) {
        return date.getHours() - 12;
      } else {
        return date.getHours();
      }
    };
    return minutes = function(date) {
      return date.getMinutes();
    };
  });

}).call(this);
